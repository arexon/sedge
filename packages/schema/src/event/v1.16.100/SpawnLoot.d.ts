export type SpawnLoot = {
	/**
	 * ## Spawn Loot
	 *
	 * Spawn loot from block.
	 */
	spawn_loot?: {
		/**
		 * ### Table
		 *
		 * File path, relative to the Behavior Pack's path, to the loot table file.
		 */
		table: string
	}
}
