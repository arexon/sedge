export type TransformItem = {
	/**
	 * ## Transform Item
	 *
	 * Transforms item into another item.
	 */
	transform_item?: {
		/**
		 * ### Transform
		 *
		 * Name of the item it should transform into.
		 */
		transform: string
	}
}
