export type Unwalkable = {
	/**
	 * ## Unwalkable
	 *
	 * Sets the block as unwalkable.
	 * Mobs would not attempt to path over top of it when the value is set to true.
	 *
	 * @default false
	 */
	unwalkable?: boolean
}
