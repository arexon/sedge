export type BlockLightFilter = {
	/**
	 * ## Block Light Filter
	 *
	 * The amount of light this block will filter out.
	 * Higher value means more light will be filtered out (0 - 15).
	 *
	 * @default 0
	 */
	block_light_filter?: number
}
