{
	"tasks": {
		"dev": "cd playground && deno run -A ../mod.ts",
		"lint": "deno lint --ignore=playground && deno fmt --ignore=playground",
		"fmt": "deno fmt --ignore=playground",
		"test": "deno test -A"
	},
	"fmt": {
		"options": {
			"indentWidth": 4,
			"useTabs": true,
			"singleQuote": true
		}
	},
	"lint": {
		"rules": {
			"exclude": ["no-var", "no-explicit-any"]
		}
	},
	"importMap": "import_map.json"
}
